services:
  - type: web
    name: inventory-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: inventory-data
      mountPath: /opt/render/project/src
      sizeGB: 1
